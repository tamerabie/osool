import { createClient } from "@/lib/supabase/server";
import { createLibraryEntry } from "@/lib/admin-actions";

export const dynamic = "force-dynamic";

export default async function Library() {
  const supabase = await createClient();
  const { data: entries } = await supabase.from("library_entries").select("*").order("created_at", { ascending: false }).limit(50);
  return <div className="space-y-5"><h1 className="text-2xl font-bold">مكتبة أصول</h1><form action={createLibraryEntry} className="rounded-2xl bg-white border border-border p-5 grid md:grid-cols-2 gap-4"><h2 className="md:col-span-2 font-bold">إضافة مدخلة جديدة</h2><Field label="العنوان" name="title" required /><Field label="التصنيف" name="category" type="select" /><Field label="الجهة" name="organization" /><Field label="الوسوم (مفصولة بفواصل)" name="tags" /><Field label="الرقم المرجعي" name="reference_number" /><div className="md:col-span-2"><label className="label">المحتوى</label><textarea name="content" rows={5} required className="input" /></div><button className="md:col-span-2 rounded-xl bg-brand text-white py-2.5 font-semibold hover:bg-brand-dark">حفظ المدخلة</button></form><div className="space-y-3">{(entries ?? []).map((e: any) => <article key={e.id} className="rounded-2xl bg-white border border-border p-5"><div className="flex justify-between gap-3"><h3 className="font-bold">{e.title}</h3><span className="badge">{e.category}</span></div><p className="mt-2 text-sm text-muted line-clamp-3">{e.content}</p><div className="mt-3 text-xs text-muted">{e.organization || ""} {e.reference_number ? ` · ${e.reference_number}` : ""}</div></article>)}</div></div>;
}
function Field({ label, name, required, type = "text" }: { label: string; name: string; required?: boolean; type?: string }) { return <div><label className="label">{label}</label>{type === "select" ? <select name={name} className="input"><option value="other">أخرى</option><option value="circular">تعميم</option><option value="decision">قرار</option><option value="publication">منشور</option><option value="instructions">تعليمات</option><option value="letter">خطاب</option><option value="note">مذكرة</option></select> : <input name={name} required={required} className="input" />}</div>; }
